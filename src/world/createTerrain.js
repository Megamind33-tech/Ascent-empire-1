import { MeshBuilder, StandardMaterial, Color3, Vector3, VertexBuffer } from '@babylonjs/core';import { CONFIG } from '../config.js';export function createTerrain(scene){const ground=MeshBuilder.CreateGround('ground',{width:CONFIG.world.size,height:CONFIG.world.size,subdivisions:90},scene);const pos=ground.getVerticesData(VertexBuffer.PositionKind);for(let i=0;i<pos.length;i+=3){const x=pos[i],z=pos[i+2];let y=0;const cityCore=Math.abs(x)<250&&Math.abs(z)<250;const road=Math.abs(x)<24||Math.abs(z)<24||Math.abs(x-160)<16||Math.abs(x+160)<16||Math.abs(z-160)<16||Math.abs(z+160)<16;if(!cityCore&&!road){y+=Math.sin(x*.008)*4+Math.cos(z*.007)*3.2+Math.sin((x+z)*.004)*6.5;}const riverCut=Math.exp(-Math.pow((x-140)*.01,2))*12;if(!road)y-=riverCut;pos[i+1]=y;}ground.updateVerticesData(VertexBuffer.PositionKind,pos);ground.refreshBoundingInfo();ground.convertToFlatShadedMesh();const gm=new StandardMaterial('groundMat',scene);gm.diffuseColor=new Color3(.35,.41,.34);gm.specularColor=new Color3(.04,.04,.04);ground.material=gm;ground.receiveShadows=true;ground.metadata={buildable:true};const shore=MeshBuilder.CreateGround('shoreline',{width:500,height:180,subdivisions:20},scene);shore.position=new Vector3(-420,2.8,-420);const sm=new StandardMaterial('shoreMat',scene);sm.diffuseColor=new Color3(.46,.44,.37);shore.material=sm;return {ground,shore};}
