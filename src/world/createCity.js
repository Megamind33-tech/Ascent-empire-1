import { MeshBuilder, StandardMaterial, Color3, Vector3, Matrix, Quaternion } from '@babylonjs/core';export function createCity(scene,shadows,state){const m=mats(scene);const road=MeshBuilder.CreateGround('road-grid',{width:520,height:520,subdivisions:2},scene);road.position.y=.08;road.material=m.road;const base=MeshBuilder.CreateBox('building-base',{width:16,depth:16,height:1},scene);base.isVisible=false;const matrices=[],q=Quaternion.Identity();for(let x=-220;x<=220;x+=30){for(let z=-220;z<=220;z+=30){if(Math.abs(x)<54||Math.abs(z)<54)continue;if(Math.abs(x-160)<22||Math.abs(x+160)<22||Math.abs(z-160)<22||Math.abs(z+160)<22)continue;if(Math.random()>.72)continue;const h=10+Math.random()*62;matrices.push(Matrix.Compose(new Vector3(1,h,1),q,new Vector3(x,h*.5,z)));}}base.thinInstanceAdd(matrices);base.material=m.building;shadows.addShadowCaster(base,true);const parliament=MeshBuilder.CreateBox('parliament',{width:60,depth:40,height:18},scene);parliament.position=new Vector3(0,9,-82);parliament.material=m.civic;const cabinet=MeshBuilder.CreateBox('cabinet',{width:44,depth:28,height:16},scene);cabinet.position=new Vector3(72,8,-70);cabinet.material=m.civic;const seaport=MeshBuilder.CreateBox('seaport',{width:90,depth:26,height:6},scene);seaport.position=new Vector3(-250,3.1,-250);seaport.material=m.port;const dock1=MeshBuilder.CreateBox('dock1',{width:14,depth:48,height:2},scene);dock1.position=new Vector3(-208,4.2,-250);dock1.material=m.port;const dock2=dock1.clone('dock2');dock2.position.x=-186;createAnchors(scene,state,m);return {road,seaport,dock1,dock2};}function createAnchors(scene,state,m){const ps=[[-140,-120],[-105,-120],[-70,-120],[-35,-120],[0,-120],[35,-120],[70,-120],[105,-120],[140,-120],[-140,120],[-105,120],[-70,120],[-35,120],[0,120],[35,120],[70,120],[105,120],[140,120],[-180,0],[180,0],[-180,40],[180,40],[-180,-40],[180,-40]];state.worldRefs.constructionAnchors=ps.map(([x,z],i)=>{const pad=MeshBuilder.CreateGround(`anchor-${i}`,{width:20,height:20},scene);pad.position.set(x,.12,z);pad.material=m.anchor;pad.metadata={buildable:true};return pad;});}function mats(scene){const road=new StandardMaterial('roadMat',scene);road.diffuseColor=new Color3(.12,.125,.13);const building=new StandardMaterial('buildingMat',scene);building.diffuseColor=new Color3(.42,.44,.47);const civic=new StandardMaterial('civicMat',scene);civic.diffuseColor=new Color3(.52,.51,.48);const anchor=new StandardMaterial('anchorMat',scene);anchor.diffuseColor=new Color3(.22,.26,.28);anchor.alpha=.7;const port=new StandardMaterial('portMat',scene);port.diffuseColor=new Color3(.36,.37,.39);return {road,building,civic,anchor,port}}
