import { MeshBuilder, StandardMaterial, Color3, Vector3 } from '@babylonjs/core';export function createWater(scene){const sea=MeshBuilder.CreateGround('sea',{width:900,height:900,subdivisions:32},scene);sea.position=new Vector3(-450,4,-450);const seaMat=new StandardMaterial('seaMat',scene);seaMat.diffuseColor=new Color3(.12,.42,.62);seaMat.emissiveColor=new Color3(.03,.08,.12);seaMat.alpha=.92;seaMat.specularColor=new Color3(.5,.55,.6);sea.material=seaMat;const river=MeshBuilder.CreateRibbon('river',{pathArray:createRiverPaths(),closeArray:false,closePath:false},scene);river.position.y=4.2;const riverMat=new StandardMaterial('riverMat',scene);riverMat.diffuseColor=new Color3(.15,.48,.65);riverMat.specularColor=new Color3(.5,.55,.6);river.material=riverMat;return {sea,river,update(t){sea.position.y=4+Math.sin(t*.6)*.15;river.position.y=4.2+Math.sin(t*.9)*.08;}}}function createRiverPaths(){const left=[],right=[];for(let i=0;i<50;i++){const z=-400+i*18,x=120+Math.sin(i*.25)*18;left.push(new Vector3(x-12,0,z));right.push(new Vector3(x+12,0,z));}return [left,right]}
