export const worldConfig = Object.freeze({
  size: 1800,
  fogStart: 600,      // Moved from 300 to not block city view
  fogEnd: 1400,       // Adjusted proportionally
  waterLevel: 4,
  roadHalfWidth: 12,
  buildPadSize: 20
});
